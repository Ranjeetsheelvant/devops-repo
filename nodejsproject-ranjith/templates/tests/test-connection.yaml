apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodejsproject-ranjith.fullname" . }}-test-connection"
  labels:
    {{- include "nodejsproject-ranjith.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodejsproject-ranjith.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
