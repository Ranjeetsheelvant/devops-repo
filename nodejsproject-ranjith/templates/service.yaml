apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodejsproject-ranjith.fullname" . }}
  labels:
    {{- include "nodejsproject-ranjith.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nodejsproject-ranjith.selectorLabels" . | nindent 4 }}
