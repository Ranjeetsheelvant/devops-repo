apiVersion: v1
kind: Service
metadata:
  name: {{ include "ranjith-chart.fullname" . }}
  labels:
    {{- include "ranjith-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ranjith-chart.selectorLabels" . | nindent 4 }}
